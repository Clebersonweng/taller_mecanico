<%= form_for(@invoice_master) do |f| %>
 <% if @invoice_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoice_master.errors.count, "error") %> prohibited this invoice_master from being saved:</h2>

      <ul>
      <% @invoice_master.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :num_invoice %><br>
    <%= f.number_field :num_invoice %>
  </div>
  <div class="field">
    <%= f.label :client_id %><br>
    <%= f.number_field :client_id %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
